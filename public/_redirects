# Proxy sharing route to edge function for social media previews
/s/:slug  https://htpcqdbhktmvppfemnad.supabase.co/functions/v1/og-vehicle?slug=:slug  301

# Proxy sitemap.xml to the edge function so it's always generated live from
# the database, instead of a static file that goes stale (achado 18/08/2026:
# ficou parado desde 20/04/2026 com só 9 veículos). Status 200 = rewrite,
# mantém a URL /sitemap.xml visível pro Google, igual o robots.txt promete.
/sitemap.xml  https://htpcqdbhktmvppfemnad.supabase.co/functions/v1/sitemap  200